using FluentAssertions;
using Xunit;
using Shouldly;

namespace FluentAssertionsToShouldlyAnalyzer.Test.TestCases
{
    public class PersonTests
    {
        [Fact]
        public void Address_Should_NotBeSameAs_NewReference()
        {
            var person = new Person(30, "John", new Address());
            var differentAddress = new Address();
            person.Address.ShouldNotBeSameAs(differentAddress);
        }
    }

    // ...existing Person and Address classes...
}