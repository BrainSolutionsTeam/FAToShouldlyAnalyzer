using FluentAssertions;
using Xunit;

namespace FluentAssertionsToShouldlyAnalyzer.Test.TestCases
{
    public class PersonTests
    {
        [Fact]
        public void Address_Should_BeSameAs_Reference()
        {
            var address = new Address();
            var person = new Person(30, "John", address);
            person.Address.{|#0:Should|}().BeSameAs(address);
        }
    }

    // ...existing Person and Address classes...
}